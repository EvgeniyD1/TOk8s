# PostgreSQL StatefulSet Service
apiVersion: v1
kind: Service
metadata:
  name: postgres-db-service
spec:
  selector:
    app: postgresql-db
  type: LoadBalancer
  ports:
    - port: 5433
      targetPort: 5432